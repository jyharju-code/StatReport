# Optional: provide the key via environment instead of the GUI.
# The GUI stores it locally in ~/.statreport/config.json (chmod 600) instead.
GEMINI_API_KEY=
# Optional model overrides:
# GEMINI_MODEL=gemini-2.5-flash            # builds the report recipe
# GEMINI_NARRATIVE_MODEL=gemini-2.5-pro    # writes the prose around verified numbers
